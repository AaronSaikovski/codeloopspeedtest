#include<stdio.h>

int main() {

    // int n=0;
    // while(n < 1000000000) {
    //     n++;
    // }
    // printf("%d ", n);
    // return 0;

    //optimised code - for loop instead of a while loop - ChatGPT!
    int n;
    for(n=0;n < 1000000000;n++){
        //do nothing
    }
    printf("%d ", n);
    return 0;
}